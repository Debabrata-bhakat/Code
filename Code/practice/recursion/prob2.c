#include<stdio.h>
#include<math.h>
char A[100];
int n;
void main()
{
    printf("Enter number\n");
    int x;
    scanf("%d",%n);
    x=(int)(pow(2,n)-1);
    for(int i=0;i<=x;i++)
    printf("%s",A); 
}

void bin(int a)
{
    if(a==1)
    A[n]=49;
    if else(a==0)
    A[n]=48;
    else
    {
            
